<nav class="navbar navbar-expand-lg navbar-dark">
  <div class="container p-0 ">

    <div class="row" style="width: 100vw;">
      <div class="col-9">
        <a class="navbar-brand" href="/general" style="margin-left: 5%;margin-top: 2%;"><i class="fa-solid fa-dice-d6"></i> Test analisis</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
          aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
      </div>

      <div class="col-3">
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0" style="margin-left: 10%;">
            {{#if user}}
              <li class="nav-item">
                <a class="navbar-brand" href="/logout">Cerrar sesión</a>
              </li>

              {{#if user.administrador}}
                <li class="nav-item">
                  <a class="navbar-brand" href="profileAdmin">Perfil</a>
                </li>
              {{else}}
                <li class="nav-item">
                  <a class="navbar-brand" href="profileUser">Perfil</a>
                </li>
              {{/if}}

              {{else}}
                <a class="navbar-brand" href="/signin">Iniciar sesión</a>

                <a class="navbar-brand" href="/signup">Registrarse</a>
              {{/if}}
          </ul>
        </div>
      </div>
    </div>
  </div>
</nav>

<div class="container mt-5 p-1">
  <div class="card p-2">

    <div class="row" style="margin-top: 5%;margin-left: 20%;margin-bottom: 5%;">
        <form action="/profileUser" method="post">
          <div class="col">
              <input type="number" min="1" max="5" name="preguntaTest">
              <input type="number" min="1" max="3" name="preguntaEncuesta">
          </div>
          <div class="col">
              <button class="btn btn-success">
                  Sapo
              </button>
          </div>
        </form>

        {{#if coeficiente}}
          <h1>El coeficiente de correlacion es {{coeficiente}}</h1>
        {{else}}
          <h1>Calcule el coeficiente con las opciones de arriba</h1>
        {{/if}}

    </div>

  </div>
</div>